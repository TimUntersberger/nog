/// Creates a new popup with given settings.
/// 
/// ```
/// type PopupSettings = #{
///   text?: String | String[],
///   padding?: Number
/// }
/// ```
///
/// @param settings PopupSettings
/// @example
/// nog.popup.create(#{ text: "Hello World" })
/// @example
extern fn create(settings)

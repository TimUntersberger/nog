# create

Creates a new popup with given settings.

## Signature

```nogscript
fn create(settings: #{
  text: String | String[],
  padding: Number
}) -> String
```

## Example

```nogscript
nog.popup.create(#{ text: "Hello World" })
```
